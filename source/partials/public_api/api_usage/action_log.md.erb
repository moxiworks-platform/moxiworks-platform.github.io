### ActionLog

Moxi Works Action Logs record actions that Contacts take that an agent might want to see to increase their effectivity. For example, if a Contact sends an email to an Agent asking a question, Moxi Works Actions Logs will record that interaction and display it to the Agent. 

<%= partial "partials/public_api/api_usage/action_log/create.md.erb" %>

<%= partial "partials/public_api/api_usage/action_log/index.md.erb" %>
